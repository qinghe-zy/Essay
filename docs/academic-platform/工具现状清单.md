# 工具现状清单

## 当前状态总览

| 组件 | 作用 | 当前状态 | 真实证据 | 备注 |
|---|---|---|---|---|
| Zotero Desktop | 主文献库 | 已可用 | 本机已安装 `C:\Program Files\Zotero\zotero.exe`，Zotero MCP 可正常调用 | 适合作为唯一主文献库 |
| Better BibTeX | 稳定导出与 citation key 管理 | 已可用 | `extensions.json` 中 `active: true`，`prefs.js` 已写入 Better BibTeX 配置项 | 当前库为空，尚未做真实条目导出演练 |
| Zotero Connector（Edge） | 浏览器侧采集文献元数据 | 部分可用 | Edge 扩展目录检测到 `Zotero Connector 5.0.199` | 需要你在真实 CNKI 页面上完成一次人工点击验证 |
| CNKI 页面采集支持 | 中文文献主来源采集 | 部分可用 | 官方 `zotero/translators` 仓库存在 `CNKI.js` translator，目标域名为 `cnki.net` | 真实采集仍依赖 CNKI 页面可访问、账号状态和页面结构 |
| `paper-search` MCP | 公开文献补充检索 | 已可用 | `codex.cmd mcp list` 可见，最小测试通过 | 不替代 CNKI 中文主来源 |
| Crossref 核验脚本 | 外文文献真实性核验 | 已可用 | 已新增 `scripts/verify_reference.py` 并完成示例运行 | 适合核验带 DOI 的期刊/会议文献 |
| Doc2X 网页入口 | PDF 结构化补充方案 | 部分可用 | 官网、API 文档、开放平台可访问 | 正式 API / MCP 仍需 `DOC2X_API_KEY` |
| 文献事实卡片模板 | 证据沉淀 | 已可用 | 已新增 `literature/templates/文献事实卡片模板.md` | 模板已具备使用条件 |
| 中文文献卡片样例 | 入库后的卡片填写参照 | 待补齐 | 当前 `literature/notes/` 目录为空 | 需要一个不伪造文献内容的空白样例 |
| 外文核验记录模板 | 保存 Crossref 核验结果 | 待补齐 | 当前只有脚本，没有统一记录模板 | 需要补一份可直接复用的 Markdown 模板 |

## 逐项结论
### 1. Zotero
结论：已可用。

说明：

- 已安装、可启动
- 本地 API 已启用
- Zotero MCP 已可调用

### 2. Better BibTeX
结论：已可用。

说明：

- 已安装到当前 Zotero Profile
- 当前状态为激活
- 已出现 Better BibTeX 配置项

### 3. Zotero Connector
结论：部分可用。

说明：

- 这台机器上检测到 Edge 版 Zotero Connector
- 但没有在真实 CNKI 页面上完成自动点击验收
- 因为 CNKI 页面访问、登录、学校权限属于人工环节

### 4. `paper-search`
结论：已可用。

说明：

- 适合补充开放外文文献与部分公开中文资料
- 不应替代 CNKI 做中文主来源

### 5. Crossref 相关能力
结论：已可用。

说明：

- 当前不是 MCP，而是一个轻量 Python 脚本
- 足够满足“标题 / 作者 / 年份 / DOI / 期刊或会议名称”核验需求

### 6. Doc2X
结论：部分可用，待人工补充。

说明：

- 已确认网页入口和 API 文档可访问
- 但没有真实 API Key，因此不能写成“已接入”

### 7. 文献整理目录与模板
结论：部分可用。

说明：

- 已建立 `literature/` 目录结构
- 已提供文献事实卡片模板
- `literature/cnki/`、`literature/foreign/`、`literature/notes/` 目前仍是待填充目录
- 当前还没有任何真实文献事实卡片样本
